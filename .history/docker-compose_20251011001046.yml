version: "3.8"

services:
  backend:
    build: ./backend
    container_name: busopt-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: busopt-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
